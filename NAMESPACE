useDynLib(gpRepel)

export(
    gpRPeakmask,
    gpRPeaklambda2mask,
    gpRPeak2maskopt,
    gpRPeak2mask,
    peaks2one,
    gpRPremave,
    gpRPostmave,
    gpRDiff,
    gpRDiffrev,
    gpRAvgall,
    gpRSdall,
    gpRUp,
    gpRDown,
    gpRBasavoff,
    gpRBasoroff,
    gpRMoverage,
    gpRMovemax,
    gpRMeanmax
    )
     
     
